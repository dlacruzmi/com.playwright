{
  "name": "reto-sauce-demo-playwright-cucumber",
  "version": "1.0.0",
  "description": "Reto de automatización QA FrontEnd con Playwright y Cucumber para Sauce Demo",
  "type": "module",
  "scripts": {
    "test": "cucumber-js --config cucumber.mjs --format json:reports/json/report.json",
    "report": "node generate-report.js"
  },
  "dependencies": {
    "@cucumber/cucumber": "^11.0.0",
    "playwright": "^1.48.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "cucumber-html-reporter": "^7.2.0"
  }
}
